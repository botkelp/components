/**
 * INTLEACHT RESEARCH LIMITED trading as BotKelp
 * Generated by BotKelp (https://botkelp.dev)
 * Generated-at: 2026-09-04T20:41:00Z
 * Repo: aiscimi-code/botkelp-components
 * Component: core-js
 *
 * BotKelp Limited Use Licence
 *
 * Copyright (c) 2026 INTLEACHT RESEARCH LIMITED trading as BotKelp.
 *
 * You may copy individual component source into your own projects, including
 * commercial projects.
 *
 * You may not:
 * - represent the BotKelp catalog or generator as your own product
 * - remove operator attribution from files as published by BotKelp
 * - redistribute assembled / generated project builds as if they were this catalog
 *
 * Generated projects include an operator notice on each page (company name,
 * generator, date, repo, and licence). A BotKelp subscriber may replace the
 * licence text with their own snippet through their account; until they do, this
 * limited-use licence remains. Generator attribution is not replaced.
 *
 * Third-party libraries named in a component keep their own licences.
 */

/**
 * Core.js polyfill initialization.
 * 
 * This module imports and configures Core.js polyfills for the application.
 * Core.js provides polyfills for ES6+ features to support older browsers.
 * 
 * See: https://github.com/zloirock/core-js
 */

// Import Core.js polyfills
import 'core-js/stable';
import 'core-js/features/promise';
import 'core-js/features/array/includes';
import 'core-js/features/array/find';
import 'core-js/features/array/find-index';
import 'core-js/features/array/filter';
import 'core-js/features/array/map';
import 'core-js/features/array/reduce';
import 'core-js/features/array/for-each';
import 'core-js/features/object/assign';
import 'core-js/features/object/entries';
import 'core-js/features/object/values';
import 'core-js/features/object/keys';
import 'core-js/features/string/includes';
import 'core-js/features/string/starts-with';
import 'core-js/features/string/ends-with';
import 'core-js/features/string/repeat';
import 'core-js/features/string/pad-start';
import 'core-js/features/string/pad-end';
import 'core-js/features/string/trim-start';
import 'core-js/features/string/trim-end';
import 'core-js/features/number/is-nan';
import 'core-js/features/number/is-finite';
import 'core-js/features/number/parse-int';
import 'core-js/features/number/parse-float';
import 'core-js/features/math/sign';
import 'core-js/features/math/trunc';
import 'core-js/features/set';
import 'core-js/features/map';
import 'core-js/features/weak-set';
import 'core-js/features/weak-map';

/**
 * Check if a specific feature is polyfilled by Core.js.
 * This is useful for debugging and conditional feature usage.
 */
export const isPolyfilled = (feature: string): boolean => {
  // Core.js adds a global __core-js_shared__ object
  if (typeof window !== 'undefined') {
    // @ts-ignore - Accessing global object
    const coreJS = window.__core_js_shared__;
    return coreJS ? coreJS.modules.some((m: string) => m.includes(feature)) : false;
  }
  return false;
};

/**
 * Get information about loaded Core.js modules.
 */
export const getCoreJSInfo = () => {
  if (typeof window !== 'undefined') {
    // @ts-ignore - Accessing global object
    const coreJS = window.__core_js_shared__;
    return {
      version: coreJS?.version || null,
      modules: coreJS?.modules || [],
      isLoaded: !!coreJS,
    };
  }
  return {
    version: null,
    modules: [],
    isLoaded: false,
  };
};
