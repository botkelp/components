/**
 * INTLEACHT RESEARCH LIMITED trading as BotKelp
 * Generated by BotKelp (https://botkelp.dev)
 * Generated-at: 2026-09-04T20:41:00Z
 * Repo: aiscimi-code/botkelp-components
 * Component: day-js
 *
 * BotKelp Limited Use Licence
 *
 * Copyright (c) 2026 INTLEACHT RESEARCH LIMITED trading as BotKelp.
 *
 * You may copy individual component source into your own projects, including
 * commercial projects.
 *
 * You may not:
 * - represent the BotKelp catalog or generator as your own product
 * - remove operator attribution from files as published by BotKelp
 * - redistribute assembled / generated project builds as if they were this catalog
 *
 * Generated projects include an operator notice on each page (company name,
 * generator, date, repo, and licence). A BotKelp subscriber may replace the
 * licence text with their own snippet through their account; until they do, this
 * limited-use licence remains. Generator attribution is not replaced.
 *
 * Third-party libraries named in a component keep their own licences.
 */

/**
 * TypeScript declarations for Day.js usage in the project.
 * 
 * This file provides type extensions and custom type definitions
 * for Day.js when used with plugins.
 */

import dayjs from 'dayjs';

// Extend Day.js with custom plugins
declare module 'dayjs' {
  interface Dayjs {
    // UTC plugin
    utc(): Dayjs;
    local(): Dayjs;
    
    // Timezone plugin
    tz(timezone: string): Dayjs;
    
    // Duration plugin
    static duration(value: number, unit?: dayjs.OpUnitType): dayjs.Duration;
    
    // RelativeTime plugin
    fromNow(withoutSuffix?: boolean): string;
    from(date: dayjs.ConfigType, withoutSuffix?: boolean): string;
    toNow(withoutSuffix?: boolean): string;
    to(date: dayjs.ConfigType, withoutSuffix?: boolean): string;
    
    // AdvancedFormat plugin
    format(template: string): string;
    
    // CustomParseFormat plugin
    static isDayjs(d: any): boolean;
    
    // isBetween plugin
    isBetween(start: dayjs.ConfigType, end: dayjs.ConfigType, granularity?: dayjs.OpUnitType, inclusivity?: '()' | '(]' | '[)' | '[]'): boolean;
    
    // isSameOrBefore plugin
    isSameOrBefore(date: dayjs.ConfigType, unit?: dayjs.OpUnitType): boolean;
    
    // isSameOrAfter plugin
    isSameOrAfter(date: dayjs.ConfigType, unit?: dayjs.OpUnitType): boolean;
  }
  
  interface Duration {
    as(unit: dayjs.OpUnitType): number;
    asSeconds(): number;
    asMinutes(): number;
    asHours(): number;
    asDays(): number;
    asWeeks(): number;
    asMonths(): number;
    asYears(): number;
    asMilliseconds(): number;
    add(value: number, unit: dayjs.OpUnitType): Duration;
    subtract(value: number, unit: dayjs.OpUnitType): Duration;
    humanize(withSuffix?: boolean): string;
  }
}
