/**
 * About view
 *
 * Source: https://router.vuejs.org/guide/
 * Verified: 2026-09-06
 */
<template>
  <section class="container">
    <h1>About</h1>
    <p>{{ appName }} is a Vue 3 + Vite + Pinia + Vue Router starter from BotKelp.</p>
  </section>
</template>

<script lang="ts">
import { defineComponent } from 'vue';

export default defineComponent({
  name: 'AboutView',
  setup() {
    const appName = import.meta.env.VITE_APP_NAME || '<%= projectName || "Vue App" %>';
    return { appName };
  },
});
</script>
