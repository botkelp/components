import type { NextConfig } from 'next';

// Components that bring barrel-export packages (e.g. lucide-react) should
// add them to experimental.optimizePackageImports here. The base config
// intentionally leaves this empty — no base dependency needs it.
const nextConfig: NextConfig = {};

export default nextConfig;
