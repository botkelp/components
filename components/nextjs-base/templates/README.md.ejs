# <%= projectName %>

Generated by [AgentScaffold](https://github.com/aiscimi-code/Agent-scaffold) from the following components:

<% components.forEach(function(id) { -%>
- `<%= id %>`
<% }); -%>

## Getting started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Environment variables

Copy `.env.example` to `.env.local` and fill in the values before running the app.
<%
const sourcedComponents = componentDetails.filter(function(c) { return c.sources.length > 0; });
if (sourcedComponents.length > 0) { -%>

## Sources

Each component below was implemented against real, current official material — not written from
memory — and checked against this project's own real `npm install && npm run build` before being
added to the registry. See [ADR 0016](https://github.com/aiscimi-code/Agent-scaffold/blob/main/docs/adr/0016-component-sources-of-truth.md).

<% sourcedComponents.forEach(function(c) { -%>
### <%- c.name %> (`<%= c.id %>`)

<% c.sources.forEach(function(s) { -%>
- [<%- s.label %>](<%- s.url %>)<% if (s.verifiedAt) { %> — verified <%= s.verifiedAt %><% } %>
<% }); -%>

<% }); -%>
<% } -%>
