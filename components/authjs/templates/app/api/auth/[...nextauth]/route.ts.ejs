/**
 * App Router Route Handler required by Auth.js.
 * Source: https://authjs.dev/getting-started/installation?framework=next.js
 * Verified 2026-09-06.
 *
 *   import { handlers } from "@/auth"
 *   export const { GET, POST } = handlers
 */
import { handlers } from '@/auth';

export const { GET, POST } = handlers;
